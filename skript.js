const options = {
    bottom: '50px', 
    right: '30px', 
    left: 'unset',
    time: '0.3s', 
    mixColor: '#fff', 
    backgroundColor: '#fff',  
    buttonColorDark: '#C8102E',  
    buttonColorLight: '#fff', 
    saveInCookies: true, 
    label: '🌓', 
    autoMatchOsTheme: true 
  }

  const darkmode = new Darkmode(options);
darkmode.showWidget();